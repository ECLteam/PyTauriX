# "$schema" = "..."  # we do not support this feature yet
identifier = "default"
description = "Capability for the main window"
windows = ["main"]
permissions = [
    "core:default",
    "pytaurix:default", # need for pytaurix ipc
    "dialog:default"
]
